# Copilot instructions

Follow the conventions in [`AGENTS.md`](../AGENTS.md) at the repository root. It is
the single source of truth for this codebase: Pydantic v2 principles, where code
belongs, and the mandatory tool + test + sample contract for every new endpoint.
Stay review-first — surface facts, never make legal/underwriting/recording decisions.
